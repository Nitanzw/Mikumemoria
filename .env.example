# Copiá este archivo a .env (o exportá las variables directamente) y
# completá con tus claves. .env está en .gitignore: nunca commitees claves.

# Clave de la API de Suno (https://sunoapi.org/api-key), usada por
# tools/generate_music_suno.py para generar la música del juego.
SUNO_API_KEY=
